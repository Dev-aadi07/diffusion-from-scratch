# Step 07 - First CNN

## Goal

Move from fully connected neural networks to convolutional neural networks (CNNs).

Unlike previous models, CNNs keep the image structure intact and process neighboring pixels together.

This is the first step toward architectures used in modern image generation models.

## Learning

- What is a convolution layer
- Why CNNs are better for images
- How image shape is preserved during processing